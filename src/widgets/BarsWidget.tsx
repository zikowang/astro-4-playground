import Bars from "components/Bars/Bars";
import Bootstrap from "components/Bootstrap";

const BarsWidget = () => {
    return (
        <Bootstrap widget="bars">
            <Bars />
        </Bootstrap>
    );
};

export default BarsWidget;
